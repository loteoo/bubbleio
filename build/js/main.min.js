(function () {
'use strict';

function h(name, attributes) {
  var rest = [];
  var children = [];
  var length = arguments.length;

  while (length-- > 2) rest.push(arguments[length]);

  while (rest.length) {
    var node = rest.pop();
    if (node && node.pop) {
      for (length = node.length; length--; ) {
        rest.push(node[length]);
      }
    } else if (node != null && node !== true && node !== false) {
      children.push(node);
    }
  }

  return typeof name === "function"
    ? name(attributes || {}, children)
    : {
        nodeName: name,
        attributes: attributes || {},
        children: children,
        key: attributes && attributes.key
      }
}

function app(state, actions, view, container) {
  var map = [].map;
  var rootElement = (container && container.children[0]) || null;
  var oldNode = rootElement && recycleElement(rootElement);
  var lifecycle = [];
  var skipRender;
  var isRecycling = true;
  var globalState = clone(state);
  var wiredActions = wireStateToActions([], globalState, clone(actions));

  scheduleRender();

  return wiredActions

  function recycleElement(element) {
    return {
      nodeName: element.nodeName.toLowerCase(),
      attributes: {},
      children: map.call(element.childNodes, function(element) {
        return element.nodeType === 3 // Node.TEXT_NODE
          ? element.nodeValue
          : recycleElement(element)
      })
    }
  }

  function resolveNode(node) {
    return typeof node === "function"
      ? resolveNode(node(globalState, wiredActions))
      : node != null ? node : ""
  }

  function render() {
    skipRender = !skipRender;

    var node = resolveNode(view);

    if (container && !skipRender) {
      rootElement = patch(container, rootElement, oldNode, (oldNode = node));
    }

    isRecycling = false;

    while (lifecycle.length) lifecycle.pop()();
  }

  function scheduleRender() {
    if (!skipRender) {
      skipRender = true;
      setTimeout(render);
    }
  }

  function clone(target, source) {
    var out = {};

    for (var i in target) out[i] = target[i];
    for (var i in source) out[i] = source[i];

    return out
  }

  function set(path, value, source) {
    var target = {};
    if (path.length) {
      target[path[0]] =
        path.length > 1 ? set(path.slice(1), value, source[path[0]]) : value;
      return clone(source, target)
    }
    return value
  }

  function get(path, source) {
    var i = 0;
    while (i < path.length) {
      source = source[path[i++]];
    }
    return source
  }

  function wireStateToActions(path, state, actions) {
    for (var key in actions) {
      typeof actions[key] === "function"
        ? (function(key, action) {
            actions[key] = function(data) {
              var result = action(data);

              if (typeof result === "function") {
                result = result(get(path, globalState), actions);
              }

              if (
                result &&
                result !== (state = get(path, globalState)) &&
                !result.then // !isPromise
              ) {
                scheduleRender(
                  (globalState = set(path, clone(state, result), globalState))
                );
              }

              return result
            };
          })(key, actions[key])
        : wireStateToActions(
            path.concat(key),
            (state[key] = clone(state[key])),
            (actions[key] = clone(actions[key]))
          );
    }

    return actions
  }

  function getKey(node) {
    return node ? node.key : null
  }

  function eventListener(event) {
    return event.currentTarget.events[event.type](event)
  }

  function updateAttribute(element, name, value, oldValue, isSvg) {
    if (name === "key") {
    } else if (name === "style") {
      for (var i in clone(oldValue, value)) {
        var style = value == null || value[i] == null ? "" : value[i];
        if (i[0] === "-") {
          element[name].setProperty(i, style);
        } else {
          element[name][i] = style;
        }
      }
    } else {
      if (name[0] === "o" && name[1] === "n") {
        if (!element.events) {
          element.events = {};
        }
        element.events[(name = name.slice(2))] = value;
        if (value) {
          if (!oldValue) {
            element.addEventListener(name, eventListener);
          }
        } else {
          element.removeEventListener(name, eventListener);
        }
      } else if (name in element && name !== "list" && !isSvg) {
        element[name] = value == null ? "" : value;
      } else if (value != null && value !== false) {
        element.setAttribute(name, value);
      }

      if (value == null || value === false) {
        element.removeAttribute(name);
      }
    }
  }

  function createElement(node, isSvg) {
    var element =
      typeof node === "string" || typeof node === "number"
        ? document.createTextNode(node)
        : (isSvg = isSvg || node.nodeName === "svg")
          ? document.createElementNS(
              "http://www.w3.org/2000/svg",
              node.nodeName
            )
          : document.createElement(node.nodeName);

    var attributes = node.attributes;
    if (attributes) {
      if (attributes.oncreate) {
        lifecycle.push(function() {
          attributes.oncreate(element);
        });
      }

      for (var i = 0; i < node.children.length; i++) {
        element.appendChild(
          createElement(
            (node.children[i] = resolveNode(node.children[i])),
            isSvg
          )
        );
      }

      for (var name in attributes) {
        updateAttribute(element, name, attributes[name], null, isSvg);
      }
    }

    return element
  }

  function updateElement(element, oldAttributes, attributes, isSvg) {
    for (var name in clone(oldAttributes, attributes)) {
      if (
        attributes[name] !==
        (name === "value" || name === "checked"
          ? element[name]
          : oldAttributes[name])
      ) {
        updateAttribute(
          element,
          name,
          attributes[name],
          oldAttributes[name],
          isSvg
        );
      }
    }

    var cb = isRecycling ? attributes.oncreate : attributes.onupdate;
    if (cb) {
      lifecycle.push(function() {
        cb(element, oldAttributes);
      });
    }
  }

  function removeChildren(element, node) {
    var attributes = node.attributes;
    if (attributes) {
      for (var i = 0; i < node.children.length; i++) {
        removeChildren(element.childNodes[i], node.children[i]);
      }

      if (attributes.ondestroy) {
        attributes.ondestroy(element);
      }
    }
    return element
  }

  function removeElement(parent, element, node) {
    function done() {
      parent.removeChild(removeChildren(element, node));
    }

    var cb = node.attributes && node.attributes.onremove;
    if (cb) {
      cb(element, done);
    } else {
      done();
    }
  }

  function patch(parent, element, oldNode, node, isSvg) {
    if (node === oldNode) {
    } else if (oldNode == null || oldNode.nodeName !== node.nodeName) {
      var newElement = createElement(node, isSvg);
      parent.insertBefore(newElement, element);

      if (oldNode != null) {
        removeElement(parent, element, oldNode);
      }

      element = newElement;
    } else if (oldNode.nodeName == null) {
      element.nodeValue = node;
    } else {
      updateElement(
        element,
        oldNode.attributes,
        node.attributes,
        (isSvg = isSvg || node.nodeName === "svg")
      );

      var oldKeyed = {};
      var newKeyed = {};
      var oldElements = [];
      var oldChildren = oldNode.children;
      var children = node.children;

      for (var i = 0; i < oldChildren.length; i++) {
        oldElements[i] = element.childNodes[i];

        var oldKey = getKey(oldChildren[i]);
        if (oldKey != null) {
          oldKeyed[oldKey] = [oldElements[i], oldChildren[i]];
        }
      }

      var i = 0;
      var k = 0;

      while (k < children.length) {
        var oldKey = getKey(oldChildren[i]);
        var newKey = getKey((children[k] = resolveNode(children[k])));

        if (newKeyed[oldKey]) {
          i++;
          continue
        }

        if (newKey == null || isRecycling) {
          if (oldKey == null) {
            patch(element, oldElements[i], oldChildren[i], children[k], isSvg);
            k++;
          }
          i++;
        } else {
          var keyedNode = oldKeyed[newKey] || [];

          if (oldKey === newKey) {
            patch(element, keyedNode[0], keyedNode[1], children[k], isSvg);
            i++;
          } else if (keyedNode[0]) {
            patch(
              element,
              element.insertBefore(keyedNode[0], oldElements[i]),
              keyedNode[1],
              children[k],
              isSvg
            );
          } else {
            patch(element, oldElements[i], null, children[k], isSvg);
          }

          newKeyed[newKey] = children[k];
          k++;
        }
      }

      while (i < oldChildren.length) {
        if (getKey(oldChildren[i]) == null) {
          removeElement(element, oldElements[i], oldChildren[i]);
        }
        i++;
      }

      for (var i in oldKeyed) {
        if (!newKeyed[i]) {
          removeElement(element, oldKeyed[i][0], oldKeyed[i][1]);
        }
      }
    }
    return element
  }
}

function Link(props, children) {
  var to = props.to;
  var location = props.location || window.location;

  props.href = to;
  props.onclick = function(e) {
    if (
      e.button !== 0 ||
      e.altKey ||
      e.metaKey ||
      e.ctrlKey ||
      e.shiftKey ||
      props.target === "_blank" ||
      e.currentTarget.origin !== location.origin
    ) {
    } else {
      e.preventDefault();

      if (to !== location.pathname) {
        history.pushState(location.pathname, "", to);
      }
    }
  };

  return h("a", props, children)
}

function wrapHistory(keys) {
  return keys.reduce(function(next, key) {
    var fn = history[key];

    history[key] = function(data, title, url) {
      fn.call(this, data, title, url);
      dispatchEvent(new CustomEvent("pushstate", { detail: data }));
    };

    return function() {
      history[key] = fn;
      next && next();
    }
  }, null)
}

var location$1 = {
  state: {
    pathname: window.location.pathname,
    previous: window.location.pathname
  },
  actions: {
    go: function(pathname) {
      history.pushState(null, "", pathname);
    },
    set: function(data) {
      return data
    }
  },
  subscribe: function(actions) {
    function handleLocationChange(e) {
      actions.set({
        pathname: window.location.pathname,
        previous: e.detail
          ? (window.location.previous = e.detail)
          : window.location.previous
      });
    }

    var unwrap = wrapHistory(["pushState", "replaceState"]);

    addEventListener("pushstate", handleLocationChange);
    addEventListener("popstate", handleLocationChange);

    return function() {
      removeEventListener("pushstate", handleLocationChange);
      removeEventListener("popstate", handleLocationChange);
      unwrap();
    }
  }
};

const timeSince = (date) => {

  let seconds = Math.floor((new Date() - date) / 1000);

  let interval = Math.floor(seconds / 31536000);

  if (interval >= 1) {
    return interval + " years ago";
  }
  interval = Math.floor(seconds / 2592000);
  if (interval >= 1) {
    return interval + " months ago";
  }
  interval = Math.floor(seconds / 86400);
  if (interval == 1) {
    return "yesterday";
  }
  if (interval >= 1) {
    return interval + " days ago";
  }
  interval = Math.floor(seconds / 3600);
  if (interval >= 1) {
    return interval + " hours ago";
  }
  interval = Math.floor(seconds / 60);
  if (interval >= 1) {
    return interval + " minutes ago";
  }

  if (seconds > 1) {
    return Math.floor(seconds) + " seconds ago";
  } else {
    return "now";
  }

};





const isElementInViewport = (el) => {
  let rect = el.getBoundingClientRect();
  return (
    rect.top >= 0 &&
    rect.left >= 0 &&
    rect.bottom <= (window.innerHeight || document.documentElement.clientHeight) &&
    rect.right <= (window.innerWidth || document.documentElement.clientWidth)
  );
};


const ObjectId = (m = Math, d = Date, h = 16, s = s => m.floor(s).toString(h)) => s(d.now() / 1000) + ' '.repeat(h).replace(/./g, () => s(m.random() * h));



const getStateFromStorage = () => JSON.parse(window.localStorage.getItem('bubbleio'));

const storeStateInStorage = (state) => {
  window.localStorage.setItem('bubbleio', JSON.stringify(state));
  return state;
};



const shortenText = (s, n) => {
  if (s.length > n+3) {
    let cut = s.indexOf(' ', n);
    if(cut == -1) return s;
    return s.substring(0, cut) + "..."
  } else {
    return s
  }
};



const onKeyDown = e => {
  if (e.key == 'ArrowUp' || e.key == 'ArrowDown') {
    let currentThread = document.querySelector('.bubble-view .thread[current="true"]');
    let scrollIntoViewOptions = {
      behavior: "auto",
      block: "center",
      inline: "nearest"
    };
    if (e.key == 'ArrowUp') {
      if (currentThread.previousSibling) {
        currentThread.previousSibling.click();
        currentThread.previousSibling.scrollIntoView(scrollIntoViewOptions);
      }
    } else if (e.key == 'ArrowDown') {
      if (currentThread.nextSibling) {
        currentThread.nextSibling.click();
        currentThread.nextSibling.scrollIntoView(scrollIntoViewOptions);
      }
    }
  }
};










const getYoutubeId = url => {
  let regExp = /^.*((youtu.be\/)|(v\/)|(\/u\/\w\/)|(embed\/)|(watch\?))\??v?=?([^#\&\?]*).*/;
  let match = url.match(regExp);
  return (match&&match[7].length==11)? match[7] : false;
};
const getVimeoId = url => {
  let m = url.match(/^.+vimeo.com\/(.*\/)?([^#\?]*)/);
  return m ? m[2] || m[1] : null;
};





const panelDrag = {
  onmousedown: ev => {
    if (!('ontouchstart' in window)) { // This only triggers when screen is not a touch screen
      ev.target.parentElement.parentElement.starty = ev.clientY + ev.target.parentElement.parentElement.scrollTop;
      ev.target.parentElement.parentElement.diffy = 0;
      ev.target.parentElement.parentElement.drag = true;
    }
  },
  onmousemove: ev => {
    if (ev.target.parentElement.parentElement.drag === true && !('ontouchstart' in window)) {
      ev.target.parentElement.parentElement.diffy = (ev.target.parentElement.parentElement.starty - (ev.clientY + ev.target.parentElement.parentElement.scrollTop));
      ev.target.parentElement.parentElement.scrollTop += ev.target.parentElement.parentElement.diffy;
    }
  },
  onmouseup: ev => {
    if (ev.target.parentElement.parentElement.drag === true && !('ontouchstart' in window)) {
      ev.target.parentElement.parentElement.drag = false;
      let start = 1,
      animate = () => {
        let step = Math.sin(start);
        if (step <= 0) {
          window.cancelAnimationFrame(animate);
        } else {
          ev.target.parentElement.parentElement.scrollTop += ev.target.parentElement.parentElement.diffy * step;
          start -= 0.025;
          window.requestAnimationFrame(animate);
        }
      };
      animate();
    }
  }
};






// State merging algorithm


const mergeStates = (stateA, stateB) => {

  // Merge the threads array before merging the bubbles
  if (stateB.bubbles) {
    for (let i = 0; i < stateB.bubbles.length; i++) { // For each new bubble
      let matchFound = false;

      // Scan for matches
      if (stateA.bubbles) {
        for (let j = 0; j < stateA.bubbles.length; j++) {
          if (stateB.bubbles[i]._id == stateA.bubbles[j]._id) {
            matchFound = true;
            stateA.bubbles[j] = mergeBubbles(stateA.bubbles[j], stateB.bubbles[i]);
          }
        }
      }

      // If there is no match -> we're adding a new bubble
      // If the new bubble has a name -> this is NOT just a thread update keyed by bubble ID
      if (!matchFound && stateB.bubbles[i].name) {
        if (!stateB.bubbles[i].threads) {
          stateB.bubbles[i].threads = [];
        }
        // Sort this bubble's threads before merging
        stateB.bubbles[i].threads = sortByRelevance(stateB.bubbles[i].threads);

        stateA.bubbles.push(stateB.bubbles[i]);
      }
    }
  }


  stateB.bubbles = stateA.bubbles; // TODO: Optimize this (currently the array gets passed around 3 times, should be 1 time only)


  // Merge user props
  stateB.user = Object.assign({}, stateA.user, stateB.user);

  // Merge the state, save in storage and return
  return storeStateInStorage(Object.assign({}, stateA, stateB));
};










const mergeBubbles = (bubbleA, bubbleB) => {

  // TODO: Use forEach

  // Merge the threads array before merging the bubbles
  if (bubbleB.threads) {
    for (let i = 0; i < bubbleB.threads.length; i++) { // For each new bubble
      let matchFound = false;

      // Scan for matches
      if (bubbleA.threads) {
        for (let j = 0; j < bubbleA.threads.length; j++) {
          if (bubbleB.threads[i]._id == bubbleA.threads[j]._id) {
            matchFound = true;
            bubbleA.threads[j] = mergeThreads(bubbleA.threads[j], bubbleB.threads[i]);
          }
        }
      }

      if (!matchFound) {
        if (!bubbleB.threads[i].messages) {
          bubbleB.threads[i].messages = [];
        }
        bubbleA.threads.unshift(bubbleB.threads[i]); // Insert in first place
      }
    }
  }

  // Sort the threads
  if (bubbleA.threads) {
    bubbleA.threads = sortByRelevance(bubbleA.threads);
  }

  bubbleB.threads = bubbleA.threads; // TODO: Optimize this (currently the array gets passed around 3 times, should be 1 time only)

  // Merge the state
  return Object.assign({}, bubbleA, bubbleB);
};










const mergeThreads = (threadA, threadB) => {

  // TODO: Use forEach

  // Merge the messages array before merging the threads
  if (threadB.messages) {
    for (let i = 0; i < threadB.messages.length; i++) { // For each new bubble
      let matchFound = false;

      // Scan for matches
      if (threadA.messages) {
        for (let j = 0; j < threadA.messages.length; j++) { // find match
          if (threadB.messages[i]._id == threadA.messages[j]._id) {
            matchFound = true;
            threadA.messages[j] = Object.assign(threadA.messages[j], threadB.messages[i]); // Basic shallow merge
          }
        }
      }

      if (!matchFound) {
        threadA.messages.push(threadB.messages[i]);
      }
    }
  }


  // Sort the messages
  if (threadA.messages) {
    threadA.messages.sort(compareAge);
  }

  threadB.messages = threadA.messages; // TODO: Optimize this (currently the array gets passed around 3 times, should be 1 time only)

  // Merge the state
  return Object.assign({}, threadA, threadB);
};







const sortByRelevance = (threads) => {

  // Score = (P-1) / (T+2)^G
  //
  // where,
  // P = points of an item (and -1 is to negate submitters vote)
  // T = time since submission (in hours)
  // G = Gravity, defaults to 1.8


  // Calculate relevance for each thread
  for (let i = 0; i < threads.length; i++) {

    if (!threads[i].messages) {
      threads[i].messages = [];
    }
    if (!threads[i].userCount) {
      threads[i].userCount = 0;
    }
    if (!threads[i].upvoted) {
      threads[i].upvoted = 0;
    }


    threads[i].relevance = (threads[i].score + (threads[i].messages.length/2) + 1) / Math.pow(((new Date() - threads[i].created) / 3600000), 1.8);

  }

  // Sort threads by "relevance"
  return threads.sort(compareRelevance);
};


const compareRelevance = (a, b) => {
  if (a.relevance < b.relevance) {
    return 1;
  }
  if (a.relevance > b.relevance) {
    return -1;
  }
  return 0;
};


const compareAge = (a, b) => {
  if (a.created < b.created) {
    return 1;
  }
  if (a.created > b.created) {
    return -1;
  }
  return 0;
};

const state = getStateFromStorage() || {
  user: {
    layoutMode: "compact"
  },
  bubbles: [],
  currentView: "userView",
  currentBubble: null,
  currentThread: null,
  bubbleForm: {}
};

const actions = {
  location: location$1.actions,
  updateState: newState => state => {
    // console.log("Update state");
    // console.log(newState);
    return mergeStates(state, newState)
  },
  deleteBubble: bubbleToRemove => state => {

    state.bubbles = state.bubbles.filter(bubble => bubble._id != bubbleToRemove._id);

    storeStateInStorage(state);

    return {
      bubbles: state.bubbles
    }
  },
  deleteThread: threadToRemove => state => {

    for (var i = 0; i < state.bubbles.length; i++) {
      if (state.bubbles[i]._id == threadToRemove.bubble_id) {
        state.bubbles[i].threads = state.bubbles[i].threads.filter(thread => thread._id != threadToRemove._id);
      }
    }
    storeStateInStorage(state);
    return {
      bubbles: state.bubbles
    }
  }
};

const BubbleForm = () => (state, actions) =>
  h('div', {class: "overlay"}, [
    h('div', {class: "inner"}, [
      h('div', {class: "close", onclick: ev => {
          ev.target.parentElement.parentElement.classList.remove("opened");
      }}),
    h('form', {class: "bubbleForm", error: state.bubbleForm.error, onsubmit: ev => {
          ev.preventDefault();


          if (ev.target.title.value) {

            // Create the bubble object
            let bubble = {
              name: ev.target.name.value,
              title: ev.target.title.value,
              desc: ev.target.desc.value,
              visibility: ev.target.visibility.value,
              author: state.user.username,
              created: new Date().getTime()
            };

            // Send new bubble to server for validation
            socket.emit('new bubble', bubble);

            // ev.target.reset();
          }

      }}, [
        h('h2', null, ["Let's create a brand new bubble."]),
        h('p', null, ["A welcoming home for a community of any common interest"]),
        h('fieldset', null, [
          h('label', null, ["Pick a name for the bubble"]),
          h('input', {type: "text", name: "title", placeholder: "Display Name", minlength: "3", maxlength: "50", required: true})
        ]),
        h('fieldset', null, [
            h('label', null, ["Choose a unique name alias"]),
            h('input', {type: "text", name: "name", placeholder: "Lowercase letters and numbers only", pattern: "[a-z0-9]+", minlength: "3", maxlength: "50", required: true})
        ]),
        h('fieldset', null, [
          h('label', null, ["Describe this bubble and what should be posted here."]),
          h('textarea', {name: "desc", placeholder: "Bubble description...", required: true})
        ]),
        h('fieldset', null, [
          h('label', null, ["Bubble visibility"]),
          h('div', {class: "row"}, [
            h('div', {class: "radio"}, [
              h('input', {type: "radio", name: "visibility", value: "public", id: "public", checked: true}),
              h('label', {for: "public"}, ["Public"])
            ]),
            h('div', {class: "radio"}, [
              h('input', {type: "radio", name: "visibility", value: "private", id: "private"}),
              h('label', {for: "private"}, ["Private"])
            ])
          ])
        ]),
        h('button', {type: "submit"}, ["Create bubble!"])
      ])
    ])
  ]);

const UserView = ({currentBubble}) => (state, actions) =>
  h('div', {class: "user-view"}, [
    h('div', {class: "frame"}, [
      h('div', {class: "header"}, [
        h('h2', null, [state.user.username]),
        h('div', {class: "options"}, [
          h('button', {onclick: ev => {
            if (ev.target.nextSibling.classList.contains("opened")) {
              ev.target.nextSibling.classList.remove("opened");
            } else {
              ev.target.nextSibling.classList.add("opened");
            }
          }}
          ),
          h('ul', null, [
            LayoutModeSwitcher(),
            h('li', {onclick: ev => {
              console.log("logout");
              localStorage.clear();
              location.reload();
            }}, [h('span', null, ["Log out"])])
          ])
        ])
      ]),
      h('ul', {class: "menu"}, [
        h('li', null, [
          h('span', null, ["My bubbles"]),
          h('ul', {class: "bubbles"}, [
            state.bubbles.map(bubble => BubbleItem({bubble: bubble, currentBubble: currentBubble})),
            h('li', {onclick: ev => {
              ev.target.nextSibling.classList.add("opened");
            }}, [
              h('span', null, ["Create bubble"]),
              BubbleForm()
            ])
          ])
        ]),
        h('li', null, [
          h('span', null, ["Multibubbles"]),
          h('ul', null, [
            h('li', null, [
              h('span', null, ["All my bubbles"])
            ]),
            h('li', null, [
              h('span', null, ["Create Multibubble"])
            ])
          ])
        ]),
        h('li', null, [
          h('span', null, ["Other"]),
          h('ul', null, [
            h('li', {onclick: ev => {
              socket.emit("random bubble");
            }}, [
              h('span', null, ["Random"])
            ])
          ])
        ])
      ]),
      h('footer')
    ])
  ]);


const BubbleItem = ({bubble, currentBubble}) =>
  h('li', {current: (bubble.name == Object.assign({"name": ""}, currentBubble).name).toString()}, [
    Link({to: "/" + bubble.name}, [
      bubble.title,
      h('div', {class: "users", userCount: bubble.userCount, onupdate: (el, oldProps) => {
        if (oldProps.userCount < bubble.userCount) {
          el.classList.add("countUp");
          void el.offsetWidth; // This forces element render
          el.classList.remove("countUp");
        }
      }}, [
        h('div', {class: "icon"}),
        h('span', {class: "count"}, [bubble.userCount])
      ])
    ])
  ]);


const LayoutModeSwitcher = () => (state, actions) => {
  if (state.user.layoutMode == "preview") {
    return (
      h('li', {onclick: ev => {
        ev.target.parentElement.parentElement.classList.remove("opened");
        actions.updateState({
          user: {
            layoutMode: "compact"
          }
        });
        console.log(state.user.layoutMode);
      }}, [h('span', null, ["Use compact layout"])])
    )
  } else {
    return (
      h('li', {onclick: ev => {
        ev.target.parentElement.parentElement.classList.remove("opened");
        actions.updateState({
          user: {
            layoutMode: "preview"
          }
        });
      }}, [h('span', null, ["Use preview layout"])])
      )
  }
};

const ThreadItem = ({thread, index, currentBubble, currentThread}) => (state, actions) =>
  h('li', {key: thread._id, class: "thread", index: index, type: thread.type, upvoted: thread.upvoted, current: (thread._id == Object.assign({'_id': ""}, currentThread)._id).toString(), hasthumbnail: (typeof thread.thumbnail !== 'undefined').toString(), onclick: ev => {
      actions.location.go("/" + currentBubble.name + "/" + thread._id);
    }, oncreate: el => {
      el.style.transform = "translateX(-100%)";
      el.style.opacity = "0";
      setTimeout(() => {
        el.removeAttribute("style");
      }, index * 40 + 40);
    }, onupdate: (el, oldProps) => {
      if (index != oldProps.index) { // If order in list changed
        el.style.transitionDuration = "0ms";
        el.style.zIndex = "1";
        el.style.transform = "translateY(calc("+(oldProps.index - index)*100+"% + "+(oldProps.index - index)*1.5+"rem))";
        void el.offsetWidth; // This forces element render
        el.removeAttribute("style");
      }
    }}, [
    ThreadInner({thread: thread, currentBubble: currentBubble})
  ]);






const ThreadInner = ({thread, currentBubble}) => (state, actions) => {
  if (state.user.layoutMode == "preview") {
    return (
      h('div', {class: "inner"}, [
        ThreadHeader({thread: thread, currentBubble: currentBubble}),
        ThreadPreview({thread: thread, currentBubble: currentBubble}),
        ThreadFooter({thread: thread})
      ])
    )
  } else { // Compact mode
    return (
      h('div', {class: "inner"}, [
        h('div', {class: "thumbnail", Style: "background-image: url('"+thread.thumbnail+"')"}),
        h('div', {class: "info"}, [
          ThreadHeader({thread: thread, currentBubble: currentBubble}),
          ThreadFooter({thread: thread})
        ])
      ])
    )
  }
};


const ThreadHeader = ({thread, currentBubble}) =>
  h('div', {class: "header"}, [
    h('h2', null, [thread.title]),
    h('p', null, ["by " + thread.author + " on " + currentBubble.name + " " + timeSince(thread.created)])
  ]);



const ThreadPreview = ({thread, currentBubble}) => {
  if (thread.type == "default") {
    return
  } else if (thread.type == "text") {
    return TextThreadPreview({thread: thread})
  } else if (thread.type == "link") {
    return LinkThreadPreview({thread: thread})
  } else if (thread.type == "youtube") {
    return YoutubeThreadPreview({thread: thread})
  } else if (thread.type == "vimeo") {
    return VimeoThreadPreview({thread: thread})
  } else if (thread.type == "image") {
    return ImageThreadPreview({thread: thread})
  }
};





const TextThreadPreview = ({thread}) =>
  h('div', {class: "text"}, [
    shortenText(thread.text, 250)
  ]);

const LinkThreadPreview = ({thread}) =>
  h('div', {class: "link"}, [
    h('a', {href: thread.url, target: "_blank"}, [
      shortenText(thread.url, 32)
    ])
  ]);

const ImageThreadPreview = ({thread}) =>
h('div', {class: "sixteen-by-nine"}, [
  h('div', {class: "img frame"}, [
    h('img', {src: thread.src, alt: thread.title})
  ])
]);

const YoutubeThreadPreview = ({thread}) =>
h('div', {class: "sixteen-by-nine"}, [
  h('div', {class: "linkPreview youtube frame"}, [
    h('img', {src: thread.thumbnail, alt: thread.title})
  ])
]);

const VimeoThreadPreview = ({thread}) =>
h('div', {class: "sixteen-by-nine"}, [
  h('div', {class: "linkPreview vimeo frame"}, [
    h('img', {src: thread.thumbnail, alt: thread.title})
  ])
]);


const ThreadFooter = ({thread}) => (state, actions) => (
  h('div', {class: "footer"}, [
    h('div', {class: "users"}, [
      h('div', {class: "count", userCount: thread.userCount, onupdate: (el, oldProps) => {
        if (oldProps.userCount < thread.userCount) {
          el.classList.add("countUp");
          void el.offsetWidth; // This forces element render
          el.classList.remove("countUp");
        }
      }}, [
        h('span', null, [thread.userCount])
      ])
    ]),
    h('div', {class: "replies"}, [
      h('div', {class: "count", messageCount: thread.messages.length, onupdate: (el, oldProps) => {
        if (oldProps.messageCount < thread.messages.length) {
          el.classList.add("countUp");
          void el.offsetWidth; // This forces element render
          el.classList.remove("countUp");
        }
      }}, [
        h('span', null, [thread.messages.length])
      ])
    ]),
    h('div', {class: "score", score: thread.score, onupdate: (el, oldProps) => {
      if (oldProps.score < thread.score) {
        el.classList.add("countUp");
        void el.offsetWidth; // This forces element render
        el.classList.remove("countUp");
      } else if (oldProps.score > thread.score) {
        el.classList.add("countDown");
        void el.offsetWidth; // This forces element render
        el.classList.remove("countDown");
      }
    }}, [
      h('button', {class: "downvote", onclick: ev => {
        ev.stopPropagation();

        if (thread.upvoted > -5) {

          // Increase score and local upvoted count
          thread.score--;
          thread.upvoted--;

          // Update immediately on this client
          actions.updateState({
            bubbles: [
              {
                _id: thread.bubble_id,
                threads: [
                  thread
                ]
              }
            ]
          });

          // Send to server
          socket.emit('update thread', thread);

          // Start cooldown animation
          ev.target.style.pointerEvents = "none";
          setTimeout(() => {
            ev.target.style.pointerEvents = "all";
          }, 2000);
        }
      }}
      ),
      h('div', {class: "count"}, [
        h('span', null, [thread.score])
      ]),
      h('button', {class: "upvote", onclick: ev => {
        ev.stopPropagation();

        if (thread.upvoted < 5) {
          // Increase score and local upvoted count
          thread.score++;
          thread.upvoted++;

          // Update immediately on this client
          actions.updateState({
            bubbles: [
              {
                _id: thread.bubble_id,
                threads: [
                  thread
                ]
              }
            ]
          });

          // Send to server
          socket.emit('update thread', thread);

          // Start cooldown animation
          ev.target.style.pointerEvents = "none";
          setTimeout(() => {
            ev.target.style.pointerEvents = "all";
          }, 1000);
        }
      }}
      )
    ])
  ])
);

const ThreadKeyboard = ({currentBubble}) => (state, actions) =>
  h('form', {class: "keyboard threadKeyboard", 'data-mode': "default", onsubmit: ev => {
    ev.preventDefault();

    if (ev.target.title.value) {



      // Create the thread object
      let thread = {
        _id: ObjectId(),
        title: ev.target.title.value,
        score: 0,
        created: new Date().getTime(),
        type: ev.target.dataset.mode,
        author: state.user.username,
        bubble_id: currentBubble._id
      };


      if (ev.target.dataset.mode == "text") {
        thread.text = ev.target.text.value;
      } else if (ev.target.dataset.mode == "link") {
        thread.url = ev.target.link.value;
        if (thread.url.match(/^(https?\:\/\/)?(www\.youtube\.com|youtu\.?be)\/.+$/)) { // If is youtube
          thread.type = "youtube";
          thread.thumbnail = "https://img.youtube.com/vi/"+getYoutubeId(thread.url)+"/hqdefault.jpg";


        } else if (thread.url.match(/^(http\:\/\/|https\:\/\/)?(www\.)?(vimeo\.com\/)([0-9]+)$/)) {
          thread.type = "vimeo";

          fetch('http://vimeo.com/api/v2/video/' + getVimeoId(thread.url) + '.json')
            .then(response => response.json())
            .then(function(response) {
              thread.thumbnail = response[0].thumbnail_large;

              // Update here
              actions.updateState({
                bubbles: [
                  {
                    _id: thread.bubble_id,
                    threads: [
                      thread
                    ]
                  }
                ]
              });
              socket.emit('update thread', thread);
            });

        } else {

        }
      } else if (ev.target.dataset.mode == "image") {
        thread.src = ev.target.image_link.value;
        thread.thumbnail = thread.src;
      }


      // Append thread to list immediately
      actions.updateState({
        bubbles: [
          {
            _id: thread.bubble_id,
            threads: [
              thread
            ]
          }
        ]
      });


      // Send new thread to server
      socket.emit('new thread', thread);


      setTimeout(() => { // Scroll to top after the re-render/update cycle has ended (to include the new element's height)
        var threadList = document.querySelector(".bubble-view .frame");
        threadList.scrollTop = 0;
      }, 10);



      ev.target.classList.remove("opened");
      ev.target.dataset.mode = "default";
      ev.target.reset();

    }

    // return false;
  }}, [
    h('div', {class: "expander", onclick: ev => {
        if (ev.target.classList.contains("opened")) {
          ev.target.parentElement.classList.remove("opened");
            ev.target.classList.remove("opened");
        } else {
          ev.target.classList.add("opened");
        }
      }}, [
      h('div', {class: "default", onclick: ev => {
        ev.target.parentElement.parentElement.dataset.mode = "default";
        ev.target.parentElement.classList.remove("opened");
        ev.target.parentElement.parentElement.classList.add("opened");
      }}),
      h('div', {class: "text", onclick: ev => {
        ev.target.parentElement.parentElement.dataset.mode = "text";
        ev.target.parentElement.classList.remove("opened");
        ev.target.parentElement.parentElement.classList.add("opened");
      }}),
      h('div', {class: "link", onclick: ev => {
        ev.target.parentElement.parentElement.dataset.mode = "link";
        ev.target.parentElement.classList.remove("opened");
        ev.target.parentElement.parentElement.classList.add("opened");
      }}),
      h('div', {class: "image", onclick: ev => {
        ev.target.parentElement.parentElement.dataset.mode = "image";
        ev.target.parentElement.classList.remove("opened");
        ev.target.parentElement.parentElement.classList.add("opened");
      }})
    ]),
    h('input', {type: "text", name: "title", placeholder: "Type something...", class: "title", maxlength: "200"}),
    h('textarea', {name: "text", placeholder: "Type something...", maxlength: "8000"}),
    h('input', {type: "url", name: "link", placeholder: "Paste link here", class: "link"}),
    h('div', {class: "image"}, [
      h('label', {for: "image_file"}),
      h('input', {type: "file", name: "image_file", id: "image_file", accept: "image/gif, image/jpeg, image/png"}),
      h('input', {type: "url", name: "image_link", placeholder: "Paste link here"})
    ]),
    h('button', {type: "submit", class: "submit"})
  ]);

const CurrentBubble = ({currentBubble, currentThread}) => (state, actions) => {
  if (currentBubble) {
    return (
      h('div', {class: "bubble-view", name: currentBubble.name, onupdate: (el, oldProps) => {
        if (oldProps.name != currentBubble.name) {
          // User switched bubbles
          socket.emit('switch bubble', {
            prevBubbleName: oldProps.name,
            nextBubbleName: currentBubble.name
          });
          console.log("--> join bubble: " + currentBubble.name);
        }
      }, oncreate: el => {
        socket.emit('switch bubble', {
          nextBubbleName: currentBubble.name
        });
        console.log("--> join bubble: " + currentBubble.name);
      }}, [
        h('div', {class: "frame", onscroll: ev => {
            if (isElementInViewport(ev.target.lastChild)) {
              console.log("load more...");
              socket.emit('get bubble', {
                bubble_id: currentBubble._id,
                skip: currentBubble.threads.length,
                limit: 10
              });
            }
          }}, [
          h('div', {class: "bubble-header"}, [
            Link({class: "back", to: "/" + name}),
            h('h2', null, [currentBubble.title]),
            h('div', {class: "options"}, [
              h('button', {onclick: ev => {
                if (ev.target.nextSibling.classList.contains("opened")) {
                  ev.target.nextSibling.classList.remove("opened");
                } else {
                  ev.target.nextSibling.classList.add("opened");
                }
              }}
              ),
              h('ul', null, [
                NotificationToggle({currentBubble: currentBubble}),
                h('li', {onclick: ev => {
                  ev.target.parentElement.parentElement.classList.remove("opened");
                  actions.location.go("/");
                  actions.deleteBubble(currentBubble);
                  socket.emit('leave bubble', currentBubble);
                }}, [h('span', null, ["Leave bubble"])]),
                OwnerAction({currentBubble: currentBubble})
              ])
            ])
          ]),
          h('ul', {class: "threads"}, [
            currentBubble.threads.map((thread, index) => ThreadItem({thread, index, currentBubble, currentThread}))
          ]),
          ThreadKeyboard({currentBubble: currentBubble}),
          h('div', {class: "loadMore"})
        ])
      ])
    )
  } else {
    return (
      h('div', {class: "bubble-view no-bubble"}, [
        h('h2', null, ["Pick a bubble!"])
      ])
    )
  }
};



// TODO: Push notifications!
const NotificationToggle = ({currentBubble}) => (state, actions) => {
  if (currentBubble.notifications === true) {
    return (
      h('li', {onclick: ev => {
        console.log("Comming soon...");
        actions.updateState({
          bubbles: [
            {
              _id: currentBubble._id,
              notifications: false
            }
          ]
        });
      }}, [h('span', null, ["Turn off notifications"])])
    )
  } else {
    return (
      h('li', {onclick: ev => {
        console.log("Comming soon...");
        actions.updateState({
          bubbles: [
            {
              _id: currentBubble._id,
              notifications: true
            }
          ]
        });
      }}, [h('span', null, ["Turn on notifications"])])
    )
  }
};


const OwnerAction = ({currentBubble}) => (state, actions) => {
  if (currentBubble.author == state.user.username) {
    return (
      h('li', {onclick: ev => {
        ev.target.parentElement.parentElement.classList.remove("opened");
        actions.location.go("/");
        actions.deleteBubble(currentBubble);
        socket.emit('archive bubble', currentBubble);
      }}, [h('span', null, ["Delete bubble"])])
    )
  }
};

const Thread = ({currentThread, currentBubble}) => (state, actions) =>
  h('article', {key: currentThread._id, class: "thread", type: currentThread.type, upvoted: currentThread.upvoted}, [
    h('div', {class: "thread-header"}, [
      h('div', {class: "back", onclick: ev => {
        actions.location.go("/" + currentBubble.name);
      }}),
    ThreadFullHeader({thread: currentThread, currentBubble: currentBubble}),
      h('div', {class: "options"}, [
        h('button', {onclick: ev => {
            if (ev.target.nextSibling.classList.contains("opened")) {
              ev.target.nextSibling.classList.remove("opened");
            } else {
              ev.target.nextSibling.classList.add("opened");
            }
          }}
        ),
        h('ul', null, [
          NotificationToggle$1({currentThread: currentThread}),
          h('li', {onclick: ev => {
            console.log("Saving comming soon...");
          }}, [h('span', null, ["Save"])]),
          DeleteThread({currentThread: currentThread})
        ])
      ])
    ]),
    h('div', {class: "panelScroll"}, [
      h('div', {class: "panel", onmousedown: panelDrag.onmousedown, onmousemove: panelDrag.onmousemove, onmouseup: panelDrag.onmouseup, onmouseout: panelDrag.onmouseup}, [
        ThreadContent({thread: currentThread, currentBubble: currentBubble}),
        ThreadFooter({thread: currentThread})
      ])
    ])
  ]);



const DeleteThread = ({currentThread}) => (state, actions) => {
  if (currentThread.author == state.user.username) { // If user owns this thread
    return (
      h('li', {onclick: ev => {
        actions.deleteThread(currentThread);
        socket.emit('archive thread', currentThread);
      }}, [h('span', null, ["Delete"])])
    )
  }
};

const NotificationToggle$1 = ({currentThread}) => (state, actions) => {
  if (currentThread.notifications === true) {
    return (
      h('li', {onclick: ev => {
        console.log("Comming soon...");
        actions.updateState({
          bubbles: [
            {
              _id: currentThread.bubble_id,
              threads: [
                {
                  _id: currentThread._id,
                  notifications: false
                }
              ]
            }
          ]
        });
      }}, [h('span', null, ["Turn off notifications"])])
    )
  } else {
    return (
      h('li', {onclick: ev => {
        console.log("Comming soon...");
        actions.updateState({
          bubbles: [
            {
              _id: currentThread.bubble_id,
              threads: [
                {
                  _id: currentThread._id,
                  notifications: true
                }
              ]
            }
          ]
        });
      }}, [h('span', null, ["Turn on notifications"])])
    )
  }
};

const ThreadFullHeader = ({thread, currentBubble}) =>
  h('div', {class: "header"}, [
    h('h2', null, [thread.title]),
    h('p', null, ["by " + thread.author + " on " + currentBubble.name + " " + timeSince(thread.created)])
  ]);


const ThreadContent = ({thread, currentBubble}) => {
  if (thread.type == "default") {
    return
  } else if (thread.type == "text") {
    return TextThreadContent({thread: thread})
  } else if (thread.type == "link") {
    return LinkThreadContent({thread: thread})
  } else if (thread.type == "youtube") {
    return YoutubeThreadContent({thread: thread})
  } else if (thread.type == "vimeo") {
    return VimeoThreadContent({thread: thread})
  } else if (thread.type == "image") {
    return ImageThreadContent({thread: thread})
  }
};





const TextThreadContent = ({thread}) =>
  h('div', {class: "text"}, [
    thread.text
  ]);

const LinkThreadContent = ({thread}) =>
  h('div', {class: "link"}, [
    h('a', {href: thread.url, target: "_blank"}, [
      shortenText(thread.url, 32)
    ])
  ]);

const ImageThreadContent = ({thread}) =>
  h('a', {href: thread.src, target: "_blank", class: "img"}, [
    h('img', {src: thread.src, alt: thread.title})
  ]);

const YoutubeThreadContent = ({thread}) =>
  h('a', {href: thread.url, target: "_blank", class: "linkPreview youtube"}, [
    h('img', {src: thread.thumbnail, alt: thread.title})
  ]);

const VimeoThreadContent = ({thread}) =>
  h('a', {href: thread.url, target: "_blank", class: "linkPreview vimeo"}, [
    h('img', {src: thread.thumbnail, alt: thread.title})
  ]);

const MessageKeyboard = ({currentThread}) => (state, actions) =>
  h('form', {class: "keyboard messageKeyboard", 'data-mode': "default", onsubmit: ev => {
      ev.preventDefault();


      if (ev.target.title.value) {

        // Create the message object
        let message = {
          _id: ObjectId(),
          bubble_id: currentThread.bubble_id,
          thread_id: currentThread._id,
          sender: state.user.username,
          message: ev.target.title.value,
          created: new Date().getTime()
        };

        // Append message to list immediately
        actions.updateState({
          bubbles: [
            {
              _id: message.bubble_id,
              threads: [
                {
                  _id: message.thread_id,
                  messages: [
                    message
                  ]
                }
              ]
            }
          ]
        });


        // Send new message to server
        socket.emit('new message', message);


        ev.target.dataset.mode = "default";
        ev.target.reset();
      }

      return false;
    }}, [
    h('div', {class: "expander", onclick: ev => ev.target.classList.contains("opened") ? ev.target.classList.remove("opened") : ev.target.classList.add("opened")}, [
      h('div', {class: "default", onclick: ev => { ev.target.parentElement.parentElement.dataset.mode = "default"; ev.target.parentElement.classList.remove("opened"); }}),
      h('div', {class: "text", onclick: ev => { ev.target.parentElement.parentElement.dataset.mode = "text"; ev.target.parentElement.classList.remove("opened"); }}),
      h('div', {class: "link", onclick: ev => { ev.target.parentElement.parentElement.dataset.mode = "link"; ev.target.parentElement.classList.remove("opened"); }}),
      h('div', {class: "image", onclick: ev => { ev.target.parentElement.parentElement.dataset.mode = "image"; ev.target.parentElement.classList.remove("opened"); }})
    ]),
    h('input', {type: "text", name: "title", placeholder: "Type something...", class: "title"}),
    h('textarea', {name: "text", placeholder: "Type something..."}),
    h('input', {type: "text", name: "link", placeholder: "Paste link here", class: "link"}),
    h('div', {class: "image"}, [
      h('label', {for: "image_file"}),
      h('input', {type: "file", name: "image_file", id: "image_file"}),
      h('input', {type: "text", name: "image_link", placeholder: "Paste link here"})
    ]),
    h('button', {type: "submit", class: "submit"})
  ]);

const CurrentThread = ({currentThread, currentBubble}) => (state, actions) => {
  if (currentThread) {
    return (
      h('div', {class: "thread-view", _id: currentThread._id, bubble_id: currentThread.bubble_id, onupdate: (el, oldProps) => {
        if (oldProps._id != currentThread._id) {
          // User switched thread
          socket.emit('switch thread', {
            prevThread: oldProps,
            nextThread: currentThread
          });
          console.log("--> join thread: " + currentThread._id);
        }
      }, oncreate: el => {
        socket.emit('switch thread', {
          nextThread: currentThread
        });
        console.log("--> join thread: " + currentThread._id);
      }}, [
        h('div', {class: "frame"}, [

          Thread({currentThread: currentThread, currentBubble: currentBubble}),

          h('div', {class: "scroller", onscroll: ev => {
            if (isElementInViewport(ev.target.children[0].lastChild)) {
              console.log("Load more not working yet");
            }
          }, messageCount: currentThread.messages.length, onupdate: (el, oldProps) => {
            // If there is a new message
            if (oldProps.messageCount < currentThread.messages.length) {
              // Scroll down message list
              el.scrollTop = el.scrollHeight;
            }
          }}, [
            h('ul', {class: "messages"}, [
              currentThread.messages.map((message, index) => MessageItem({message, index})),
              h('li', {class: "loadMoreMessages"})
            ])
          ]),

          MessageKeyboard({currentThread: currentThread})
        ])
      ])
    )
  } else if (currentBubble) {
    return (
      h('div', {class: "thread-view no-thread"}, [
        h('h2', null, ["Now pick a thread!"])
      ])
    )
  }
};


const MessageItem = ({message, index}) => (state, actions) => {
  return (
    h('li', {key: message._id, sent: (message.sender == state.user.username).toString(), index: index, oncreate: el => {
      el.classList.add("newmessage");
      setTimeout(() => {
        el.classList.remove("newmessage");
      }, index * 20);
    }}, [
      h('div', {class: "content"}, [message.message]),
      h('div', {class: "info"}, [message.sender + " " + timeSince(message.created)])
    ])
  )
};

const view = (state, actions) => {

  // console.log(state);

  // If logged in
  if (state.user._id) {


    let urlparts = window.location.pathname.split("/");


    // Manage mobile nav and last bubble / thread from url
    state.currentView = "userView";


    if (urlparts[1]) {

      // Load bubble from state
      state.currentBubble = state.bubbles.find(bubble => bubble.name == urlparts[1]);


      // Switch mobile view
      state.currentView = "bubbleView";

      // If the bubble doesn't exist in the state yet
      if (!state.currentBubble) {
        // Create a temporary bubble while it loads
        state.currentBubble = {
          name: urlparts[1],
          threads: []
        };
      }



      if (urlparts[2]) {

        // Load thread from state
        state.currentThread = state.currentBubble.threads.find(thread => thread._id == urlparts[2]);

        // Switch mobile view
        state.currentView = "threadView";
      }
    }





    return (
      h('div', {class: "slider " + state.currentView + " " + state.user.layoutMode}, [
        UserView({currentBubble: state.currentBubble}),
        CurrentBubble({currentBubble: state.currentBubble, currentThread: state.currentThread}),
        CurrentThread({currentBubble: state.currentBubble, currentThread: state.currentThread})
      ])
    )
  } else {
    return (
      h('form', {class: "loginForm", onsubmit: ev => {
          ev.preventDefault();
          socket.emit('login', {
            username: ev.target.username.value
          });
          return false;
        }}, [
        h('h2', null, ["Pick a name"]),
        h('input', {type: "text", placeholder: "Type here...", name: "username", minlength: "3", maxlength: "50", required: true, autofocus: true})
      ])
    )
  }
};

// Websocket connect
window.socket = io.connect(window.location.host);


// Load our hyperapp
window.main = app(state, actions, view, document.querySelector("main"));

// Activate our router
const unsubscribe = location$1.subscribe(main.location);


// Login on load (sets a username on our socket connection)
if (state.user._id) {
  socket.emit('login', {
    username: state.user.username
  });
}


// Manage socket.io events
window.socket.on('update state', newState => main.updateState(newState));

window.socket.on('delete bubble', bubble => main.deleteBubble(bubble));

window.socket.on('delete thread', thread => main.deleteThread(thread));

window.socket.on('redirect', pathname => main.location.go(pathname));


// Handle keyboard events
document.onkeydown = onKeyDown;

// // Activate the service worker
// if ('serviceWorker' in navigator) {
//   window.addEventListener('load', function() {
//     navigator.serviceWorker.register('/sw.js').then(function(registration) {
//       // Registration was successful
//       console.log('ServiceWorker registration successful with scope: ', registration.scope);
//     }, function(err) {
//       // registration failed :(
//       console.log('ServiceWorker registration failed: ', err);
//     });
//   });
// }

}());
